<% if object.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      送信した内容に<%= object.errors.count %>つエラーがあります。
    </div>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
